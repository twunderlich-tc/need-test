name: Test need command

on: 
  workflow_dispatch: {}

jobs:
  sleep:
    runs-on: ubuntu-latest
    steps:
     - name: Sleep for 30 seconds
       run: sleep 30s
       shell: bash
     - run: echo "done"
